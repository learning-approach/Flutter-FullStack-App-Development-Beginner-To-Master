import 'package:flutter/material.dart';

class RecentlyUploadedItems extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.green,
    );
  }
}
